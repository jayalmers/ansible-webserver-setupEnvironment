# ansible-webserver-setupEnvironment
Ansible playbook to install, update, and deploy an Apache24/PHP56 webserver.  There is a load balancer health-check file that is dropped into the DocumentRoot as well.
